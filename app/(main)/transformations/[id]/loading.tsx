import Loader from "@/components/shared/Loader";

const TransformationDetailsLoading = () => {
  return (
    <main className="w-full h-full flex justify-center items-center">
      <Loader />
    </main>
  );
};
export default TransformationDetailsLoading;
