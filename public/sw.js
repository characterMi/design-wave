const VERSION = "2.5.2";

const assets = [
  "/",
  "/manifest.webmanifest",
  "/assets/icons/add.svg",
  "/assets/icons/bag.svg",
  "/assets/icons/camera.svg",
  "/assets/icons/caret-down.svg",
  "/assets/icons/check.svg",
  "/assets/icons/close.svg",
  "/assets/icons/coins.svg",
  "/assets/icons/credit-coins.svg",
  "/assets/icons/cross.svg",
  "/assets/icons/download.svg",
  "/assets/icons/filter.svg",
  "/assets/icons/free-plan.svg",
  "/assets/icons/home.svg",
  "/assets/icons/image.svg",
  "/assets/icons/menu.svg",
  "/assets/icons/photo.svg",
  "/assets/icons/profile.svg",
  "/assets/icons/scan.svg",
  "/assets/icons/search.svg",
  "/assets/icons/spinner.svg",
  "/assets/icons/stars.svg",
  "/assets/images/1.jpg",
  "/assets/images/2.jpg",
  "/assets/images/3.jpg",
  "/assets/images/4.jpg",
  "/assets/images/5.jpg",
  "/assets/images/og-poster.jpg",
  "/assets/images/d1.png",
  "/assets/images/d2.png",
  "/assets/images/d3.png",
  "/assets/images/icon-192.png",
  "/assets/images/icon-512.png",
  "/assets/images/icon-maskable-192.png",
  "/assets/images/icon-maskable-512.png",
  "/assets/images/background.webp",
  "/assets/images/banner-bg.webp",
  "/assets/images/gradient-bg.svg",
  "/assets/images/logo-icon.svg",
  "/assets/images/logo-text.svg",
];

const assetsCacheName = "design-wave" + VERSION;

self.addEventListener("install", (event) => {
  event.waitUntil(
    (async () => {
      const cache = await caches.open(assetsCacheName);
      await Promise.allSettled(assets.map((url) => cache.add(url)));

      self.skipWaiting();
    })()
  );
});

self.addEventListener("activate", (event) => {
  event.waitUntil(
    (async () => {
      // Remove old caches (assets)
      const keys = await caches.keys();
      await Promise.allSettled(
        keys
          .filter((key) => key !== assetsCacheName)
          .map((key) => caches.delete(key))
      );
      await self.clients.claim();
    })()
  );
});

self.addEventListener("message", (event) => {
  // We want to cache missing assets (if there is any) whenever user loads our app.
  if (event.data?.type === "CACHE-MISSING-ASSETS") {
    (async () => {
      try {
        const cache = await caches.open(assetsCacheName);
        const matches = await Promise.all(
          assets.map((asset) => cache.match(asset))
        );

        const missingAssets = assets.filter((_, i) => !matches[i]);

        if (missingAssets.length) {
          await Promise.allSettled(missingAssets.map((url) => cache.add(url)));
        }
      } catch (err) {
        console.error("Error caching missing assets:", err);
      }
    })();
  }
});

self.addEventListener("fetch", (event) => {
  // we don't want to cache any method beside GET...
  if (event.request.method !== "GET") {
    event.respondWith(networkOnly(event.request));
    return;
  }

  const eventUrl = new URL(event.request.url);

  // Next.js related things... (i don't even know what they are, i just know they ruin my app)
  if (eventUrl.searchParams.has("_rsc")) {
    event.respondWith(networkOnly(event.request));
    return;
  }

  // HTML files, if the requested page is in the cache we respond with it, if not, we respond with the offline page...
  if (
    event.request.mode === "navigate" ||
    event.request.headers.get("accept")?.includes("text/html")
  ) {
    if (eventUrl.pathname === "/") {
      event.respondWith(
        cacheOnly(event.request, assetsCacheName, /*returnOffline= */ true)
      );
      return;
    }

    if (eventUrl.pathname === "/feed") {
      event.respondWith(
        staleWhileRevalidate(
          event.request,
          assetsCacheName,
          /*returnOffline= */ true
        )
      );
      return;
    }

    // only caching /transformations/[id] pages.
    if (/^\/transformations\/[a-zA-Z0-9]+$/.test(eventUrl.pathname)) {
      event.respondWith(
        staleWhileRevalidate(
          event.request,
          assetsCacheName,
          /*returnOffline=*/ true
        )
      );
      return;
    }

    event.respondWith(
      networkOnly(event.request, null, /*returnOffline= */ true)
    );
    return;
  }

  // static files
  if (eventUrl.host === self.location.host) {
    // static files generated by Next.js
    // (Fonts)
    if (eventUrl.pathname.startsWith("/_next/static/media")) {
      event.respondWith(cacheOnly(event.request, assetsCacheName));
      return;
    }

    // (CSS, JS)
    if (eventUrl.pathname.startsWith("/_next/static")) {
      event.respondWith(staleWhileRevalidate(event.request, assetsCacheName));
      return;
    }

    // images generated by Next.js
    if (eventUrl.pathname.startsWith("/_next/image")) {
      event.respondWith(cacheOnly(event.request, assetsCacheName));
      return;
    }

    // cached files in assets
    if (assets.includes(eventUrl.pathname)) {
      event.respondWith(cacheOnly(event.request, assetsCacheName));
      return;
    }
  }

  // other requests...
  event.respondWith(networkOnly(event.request));
});

async function cacheOnly(req, cacheName, returnOffline = false) {
  const cache = await caches.open(cacheName);
  const cachedResponse = await cache.match(req);

  if (cachedResponse) {
    return cachedResponse.clone();
  }

  return networkOnly(req, cache, returnOffline);
}

async function staleWhileRevalidate(req, cacheName, returnOffline = false) {
  const cache = await caches.open(cacheName);
  const cachedResponse = await cache.match(req);
  const fetchRes = await networkOnly(req, cache, returnOffline);

  return cachedResponse || fetchRes;
}

async function networkOnly(req, cache = null, returnOffline = false) {
  return fetch(req)
    .then(async (networkRes) => {
      if (cache && networkRes.ok) await cache.put(req, networkRes.clone());
      return networkRes;
    })
    .catch(() => {
      if (returnOffline) return offlineHTMLFallback();
      return responseFallback();
    });
}

function responseFallback() {
  return new Response(
    "Network error and no cached data available. see the browser's console for more information",
    {
      status: 503,
      statusText: "Service Unavailable.",
      headers: { "Content-Type": "text/plain" },
    }
  );
}

function offlineHTMLFallback() {
  return new Response(
    `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><meta name="viewport"content="width=device-width,initial-scale=1,maximum-scale=5,viewport-fit=cover"/><link rel="icon" href="/assets/images/icon-192.png"/><link rel="apple-touch-icon" href="/assets/images/icon-192.png"/><link rel="manifest" href="/manifest.webmanifest"/><meta name="description"content="You're offline, check your internet connection and try again."/><meta name="theme-color"content="#624cf5"/><meta name="color-scheme"content="only light"/><title>You're offline )):</title><style>body{background-color:white;font-family:Arial,Helvetica,sans-serif;display:flex;flex-direction:column;justify-content:center;align-items:center;height:100vh;height:100svh;margin:0;padding:0 1rem;overscroll-behavior:none;user-select:none;-webkit-user-select:none;user-drag:none;-webkit-user-drag:none;touch-callout:none;-webkit-touch-callout:none;}div{display:flex;align-items:center;gap:10px;}button{padding:0.75rem;border-radius:6px;font-size:0.875rem;border:none;outline:none;letter-spacing:0.025em;font-size:0.875rem;line-height:1.25rem;font-weight:500;background-color:#0f172a;color:#f8fafc;cursor:pointer;transition:background-color 0.3s ease;margin-top:10px;-webkit-tap-highlight-color:transparent;}button:hover{background-color:rgba(15,23,42,0.9);}button:focus-visible{background-color:rgba(15,23,42,0.9);}</style></head><body><div><h1 style="color:#020817">You're currently <span style="color:#624cf5">offline.</span></h1><svg stroke="#020817"fill="none"stroke-width="2"viewBox="0 0 24 24"aria-hidden="true"style="min-width:3.5rem;min-height:3.5rem"width="3.5rem"height="3.5rem"xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round"stroke-linejoin="round"d="M18.364 5.636a9 9 0 010 12.728m0 0l-2.829-2.829m2.829 2.829L21 21M15.536 8.464a5 5 0 010 7.072m0 0l-2.829-2.829m-4.243 2.829a4.978 4.978 0 01-1.414-2.83m-1.414 5.658a9 9 0 01-2.167-9.238m7.824 2.167a1 1 0 111.414 1.414m-1.414-1.414L3 3m8.293 8.293l1.414 1.414"></path></svg></div><p>it looks like you're offline, Unfortunately you can't use <span style="color:#0f172a">DesignWave</span> when offline, make sure you're online and Reload the page.</p><button onclick="window.location.reload()">Reload</button></body></html>`,
    {
      status: 503,
      statusText: "Service Unavailable.",
      headers: { "Content-Type": "text/html" },
    }
  );
}
